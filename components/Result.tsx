import React from 'react';
import { GameState, Pack } from '../types';
import { Button } from './ui/button';
import { Card } from './ui/card';
import { RotateCcw, Share2 } from 'lucide-react';
import TierBoard from './TierBoard';

interface ResultProps {
  gameState: GameState;
  pack: Pack;
  onReset: () => void;
}

const Result: React.FC<ResultProps> = ({ gameState, pack, onReset }) => {
  return (
    <div className="flex flex-col items-center justify-start min-h-screen py-8 animate-slide-up w-full max-w-4xl mx-auto space-y-8 px-4">
      <div className="text-center space-y-2">
        <h1 className="text-4xl font-extrabold text-stone-900">Vibe Check Complete</h1>
        <p className="text-stone-500">Theme: {pack.name}</p>
      </div>

      <div className="w-full bg-white p-4 rounded-xl shadow-lg border border-stone-200">
        <TierBoard items={gameState.composition} players={gameState.players} />
        <div className="mt-2 text-right">
             <span className="text-[10px] font-bold text-stone-400 uppercase tracking-widest">Generated by VibeCheck</span>
        </div>
      </div>

      <Card className="w-full bg-white/50 backdrop-blur border-stone-200">
        <div className="p-4">
            <h3 className="text-sm font-bold text-stone-500 uppercase tracking-wider mb-3">The Jury</h3>
            <div className="flex flex-wrap gap-2">
                {gameState.players.map(p => (
                    <span key={p.id} className="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-stone-100 text-stone-800 border border-stone-200">
                        {p.name}
                    </span>
                ))}
            </div>
        </div>
      </Card>

      <div className="flex gap-4 w-full max-w-md">
        <Button onClick={onReset} variant="outline" className="flex-1 border-2 border-stone-900 font-bold hover:bg-stone-100">
            <RotateCcw className="w-4 h-4 mr-2" />
            New Game
        </Button>
        <Button className="flex-1 bg-stone-900 text-white border-2 border-stone-900 shadow-[4px_4px_0px_0px_rgba(249,115,22,1)] hover:shadow-none hover:translate-y-[4px] hover:translate-x-[4px] transition-all font-bold">
            <Share2 className="w-4 h-4 mr-2" />
            Share Tier List
        </Button>
      </div>
    </div>
  );
};

export default Result;